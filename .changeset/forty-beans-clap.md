---
"attunement": minor
---

`attunement/devtools`: dev override panel generated from the schema (enum → select, boolean → checkbox) — standalone floating widget (`AttunementDevtools`) or TanStack Devtools plugin (`attunementDevtoolsPlugin`). `fromOverrides()` source layers localStorage overrides + `?config.KEY=value` URL bootstrap over real config via `merge`, validated by the schema like everything else.
