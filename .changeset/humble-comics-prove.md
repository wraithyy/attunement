---
"attunement": minor
---

`attunement/vite`: Vite plugin — dev server serves the config file (full reload on change), optional `injectKey` injects config into index.html for `fromWindow` (real content in dev, deploy-replaceable placeholder in builds).
